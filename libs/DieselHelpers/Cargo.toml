[package]
	edition = "2021"
	name = "diesel_helpers"
	version = "0.1.0"
	workspace = "../../.cargo/workspace"

[dependencies]
	don_error.workspace = true

	diesel.workspace = true
	serde = { workspace = true, features = ["derive"] }
	thiserror.workspace = true
